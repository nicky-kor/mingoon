# mingoon

아직 프로젝트 내용은 정해지지 않았습니다. 아이패드 등 어디서든 Claude Code on the web으로 바로 이어서 개발할 수 있도록 개발 환경(SessionStart 훅)만 먼저 세팅된 상태입니다.

## 개발 환경

`.claude/hooks/session-start.sh`가 세션 시작 시 자동으로 실행되어 `requirements.txt`(Python) 또는 `package.json`(Node)이 있으면 의존성을 설치합니다. 아직 프로젝트 코드가 없어서 지금은 아무 것도 설치하지 않고 넘어갑니다.
